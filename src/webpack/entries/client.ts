import * as React from "react";
import * as ReactDOM from "react-dom";
import Component from "./hotWrap";

const props = {};

ReactDOM.render(
  React.createElement(Component, props),
  document.getElementById("root")
);
